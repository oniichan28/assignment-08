
module.exports = {
  theme: {
    extend: {
      animation: {
        'spin-slow': 'spin 3s linear infinite', // 3 seconds per rotation
        'spin-slower': 'spin 5s linear infinite', // 5 seconds per rotation
      },
    },
  },
  plugins: [],
}
